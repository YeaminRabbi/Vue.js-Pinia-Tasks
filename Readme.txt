Clone & Setup 

1. Clone this project
2. npm install
3. npm run dev
4. json-server -w ./data/db.json 

NB: if json-server doesn't run for authorization then open powershell with administration and run [ Set-ExecutionPolicy RemoteSigned -Scope CurrentUser ]